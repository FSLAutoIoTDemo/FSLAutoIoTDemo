function initalizeMaps() {
    var a = {
        zoom: 15,
        center: new google.maps.LatLng(30.264534, -97.743403)
    };
    map = new google.maps.Map($("#vd-Map-obj")[0], a);
}

jQuery(document).ready(function(a) {
    initalizeMaps();
});

var map, mapContainer = $("#vd-Map-obj")[0];